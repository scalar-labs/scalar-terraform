name: Terraform test for CA module of Azure

on:
  pull_request:
    branches:
      - master
    paths:
      - modules/azure/ca/**
      - modules/universal/ca/**
      - example/azure/ca/**
      - provision/ansible/**
  push:
    branches:
      - master
    paths:
      - modules/azure/ca/**
      - modules/universal/ca/**
      - example/azure/ca/**
      - provision/ansible/**

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    env:
      TF_VERSION: 0.12.29
    defaults:
      run:
        working-directory: ./example/azure/ca

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format
        run: terraform fmt -check -recursive
        working-directory: ./modules/azure/ca

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate -no-color
